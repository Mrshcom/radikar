{
  "name": "radikar-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "prepare": "node scripts/setup-git-hooks.mjs",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@radikar/web",
    "dev:api": "turbo run dev --filter=@radikar/api",
    "dev:stack": "turbo run dev --filter=@radikar/web --filter=@radikar/api",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@radikar/web...",
    "build:api": "turbo run build --filter=@radikar/api...",
    "build:packages": "turbo run build --filter='./packages/*'",
    "build:all": "turbo run build",
    "start:web": "npm run start --workspace @radikar/web",
    "start:api": "npm run start --workspace @radikar/api",
    "test": "turbo run test",
    "test:web": "turbo run test --filter=@radikar/web",
    "test:api": "turbo run test --filter=@radikar/api",
    "test:all": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "infra:up": "docker compose up -d postgres",
    "infra:down": "docker compose down",
    "db:generate": "npm run db:generate --workspace @radikar/database",
    "db:migrate": "npm run db:migrate --workspace @radikar/database",
    "db:seed": "npm run db:seed --workspace @radikar/database",
    "knowledge:check": "node scripts/check-knowledge-base.mjs"
  },
  "devDependencies": {
    "turbo": "2.10.11"
  }
}
